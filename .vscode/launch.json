{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Debug Go Collector",
        "type": "go",
        "request": "launch",
        "mode": "debug",
        "program": "${workspaceFolder}/affirm-otelcol",
        "args": ["--config", "/workspaces/access_log_sampling/volumebasedlogsampler/config.yaml"],
        "envFile": "${workspaceFolder}/.env"
      }
    ]
  }
  